# yaml-language-server: $schema=./settings.schema.yaml

timelines:
  VHHH:
    - timelineTitle: "07"
      arrivalLabelLayoutId: VHHHARR
      right:
        runways: ["07L", "07C", "07R"]

    - timelineTitle: "25"
      arrivalLabelLayoutId: VHHHARR
      right:
        runways: ["25L", "25C", "25R"]

arrivalLabelLayouts:
  VHHHARR:
    - {src: assignedRunway, w: 4}
    - {src: callSign, w: 9}
    - {src: assignedStar, w: 4, maxLen: 3}
    - {src: timeToLoseOrGain, w: 4, align: left}
    - {src: estimatedLandingTime, w: 2, maxLen: 2, align: right}